

var isValid = function (s){

    var stack = []
    const parens = "{} [] ()"
    let i = 0;

    while (i < s.length) {
        stack.push(s[i])
    }
}

var x = {
    "}": "{"
};

var s = ["}", "}", "}", "]"];
var i = 0;  // Changed "0" to 0 to make it a number

console.log(x[s[i]]);