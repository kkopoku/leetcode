// recursion is calling a function within its own function
let i = 0;

function recursion(){

    // console.log('recursion')
    // console.log(i)
    // i += 1
    // while(i<3) recursion()

    i = []
    for (let j of i){
        console.log(j)
    }

    
}

recursion()
